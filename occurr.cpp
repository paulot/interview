// Find the number of occurrences of a number in a sorted array
#include <iostream>
#include <cstdlib>
#include <vector>

using namespace std;

int numOccurr(vector<int> &vect, int num) {

    return 0;
}

int main() {
    int arr[] = { 1,2,3,4,5 };
    vector<int> vect(arr, arr + sizeof(arr) / sizeof(int));
    numOccurr(vect, 3);
}
